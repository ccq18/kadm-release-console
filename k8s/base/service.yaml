apiVersion: v1
kind: Service
metadata:
  name: kadm
  namespace: kadm
  labels:
    app.kubernetes.io/name: kadm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kadm
  ports:
    - name: http
      port: 80
      targetPort: http
